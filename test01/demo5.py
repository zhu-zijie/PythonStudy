# coding=utf-8
# @File: demo5.py
# @Author: zijier
# @Desc: 
# @Date: 10:58 PM 2024/03/12

# 列表生成式
# lst = [i * i for i in range(10)]
# print(lst)

# items = ['Fruits', 'Books', 'Others']
# prices = [56, 64, 54]
# dic = {item: price for item, price in zip(items, prices)}
# print(dic)

# 不可变序列：字符串，元组 没有增删改的操作
# 可变序列： 列表，字典 可以对序列进行增删改操作，对象地址不发生改变

# 集合调用add()方法，一次添加一个元素
# 集合调用update()方法，至少添加一个元素
# 调用remove()方法，一次删除一个指定的元素，若不存在，则抛出KeyError
# 调用discard()方法，一次删除指定的元素
# 调用pop()方法，一次删除任意一个元素
# 调用clear()方法，清空集合
